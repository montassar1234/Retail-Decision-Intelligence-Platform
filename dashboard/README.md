# Dashboard

This project ships with a Streamlit dashboard in [`app.py`](C:\Users\a\Desktop\BI+AI Project\app.py).

For an academic presentation, you can say:

- `Streamlit` provides the interactive BI prototype used for the live demo.
- `Power BI` can be built later from the processed CSV outputs in `data/processed/`.

